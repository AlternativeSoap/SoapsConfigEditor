/** Default tier IDs from SoapsQuest tiers.yml when no file is open. */
export const DEFAULT_TIER_IDS = [
  'common',
  'uncommon',
  'rare',
  'epic',
  'legendary',
  'mythic',
] as const

/** Default difficulty IDs from SoapsQuest difficulties.yml when no file is open. */
export const DEFAULT_DIFFICULTY_IDS = [
  'easy',
  'normal',
  'hard',
  'expert',
  'nightmare',
] as const
